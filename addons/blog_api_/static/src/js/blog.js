odoo.define('my_blog.blog', function (require) {
    "use strict";
    // minimal placeholder script for the blog module
    document.addEventListener('DOMContentLoaded', function () {
        // no-op: can be extended
    });
});
